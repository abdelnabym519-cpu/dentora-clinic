name: Apply Closure Patch

on:
  push:
    branches: [phase/arabic-rtl-final-ui]

permissions:
  contents: write

jobs:
  apply:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Paused while canonical type-fix checkpoints are applied."
