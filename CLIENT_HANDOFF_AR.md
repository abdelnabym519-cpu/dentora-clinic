# Dentora — دليل التسليم والتشغيل النهائي للعميل

هذا الدليل هو نقطة البدء لفريق التركيب والدعم في العيادة. استخدمه مع:

- `CLIENT_INSTALL_AR.md` للتثبيت على Windows أو Mini PC.
- `WORKSTATION_SETUP_AR.md` لإعداد أجهزة الاستقبال والأطباء.
- `BACKUP_RESTORE_AR.md` للنسخ الاحتياطي والاستعادة والتعافي من الكوارث.
- `AUTO_UPDATE_AR.md` للتحديث الموقّع والاسترجاع عند الفشل.

## 1. قبل التسليم

لا تسلّم إلا الملفين الصادرين من عملية التغليف الرسمية لنفس Source SHA:

- `Dentora_Client.zip`
- `Dentora_Client.zip.sha256`

تحقق من SHA-256 مستقلًا، ثم افحص `BUILD_INFO.txt` داخل الحزمة. يجب أن يسجل
Source SHA وعناوين خادم الترخيص وبيانات Auto Update. الحزمة القابلة لإعادة
الاستخدام لا يجوز أن تحتوي على `.env.client` أو مفاتيح توقيع خاصة أو بيانات
مرضى أو خدمات المالك `license-server` و`license-worker`.

## 2. اختيار الجهاز

### جهاز واحد داخل غرفة العمل

استخدم Windows 10/11 x64 مع Docker Desktop وWSL2. الحد العملي 8 GB RAM،
و16 GB أفضل عند استخدام Dental3D أو ملفات تصوير كبيرة. احتفظ بمساحة منفصلة
للنسخ الاحتياطية ولا تجعل النسخة الوحيدة على نفس القرص.

### Mini PC يخدم العيادة

خصص IPv4 ثابتًا أو DHCP reservation، شغّل التثبيت الأول، ثم نفّذ
`SETUP_LAN_HTTPS.bat <PRIVATE-IP>` كمسؤول. لا تنشر PostgreSQL أو backend أو
frontend مباشرة على الشبكة؛ نقطة الدخول الوحيدة هي Caddy عبر HTTPS.

بعد نجاح فحص HTTPS، شغّل `PREPARE_WORKSTATION_KIT.bat` وانقل الحزمة الناتجة
إلى كل جهاز عمل. لا يحتاج جهاز العمل Docker أو قاعدة بيانات أو Secrets.

## 3. إعداد البيئة

أول تشغيل ينشئ `.env.client` محليًا ويولّد أسرار كل تثبيت وبصمة الجهاز. لا
تنسخ هذا الملف بين العيادات ولا تضعه في Git أو تذكرة دعم. القيم العامة المثبتة
في الحزمة هي مفاتيح تحقق فقط؛ مفاتيح توقيع الترخيص والتحديث لا توجد عند العميل.

راجع قبل التشغيل:

- `PUBLIC_URL` يطابق عنوان الوصول الفعلي وشهادة HTTPS.
- `TIMEZONE` و`CURRENCY` مناسبـان للعيادة.
- `STORAGE_BACKEND=local` للنسخة المحلية المدعومة.
- `OPENAI_API_KEY` يبقى فارغًا إذا لم يتم تفعيل Copilot.
- أي مفاتيح Evolution أو مزود خارجي تضاف محليًا ولا تُرسل في logs.

## 4. التشغيل اليومي

- التشغيل: `START_DENTORA.bat`
- الإيقاف بدون حذف البيانات: `STOP_DENTORA.bat`
- النسخ الاحتياطي: `BACKUP_DENTORA.bat`
- الاستعادة: `RESTORE_DENTORA.bat "<backup.zip>"`
- التحديث: `UPDATE_DENTORA.bat`

ممنوع استخدام `docker compose down -v` لأنه يحذف volumes. بعد أي صيانة تحقق
من `/health` ومن تسجيل الدخول بحساب محدود الصلاحيات، وليس حساب المسؤول فقط.

## 5. Backup / Restore والتعافي من الكوارث

ضع جدولًا دوريًا للنسخ، وانقل نسخة حديثة إلى وسيط مشفر خارج الجهاز. نفّذ Restore
تجريبيًا دوريًا على بيئة معزولة؛ وجود ZIP وحده لا يثبت قابليته للاستعادة.

عند تعطل القرص أو استبدال الجهاز:

1. جهز Windows وDocker Desktop على الجهاز البديل.
2. ثبت نفس إصدار Dentora المطابق للـbackup وفعّل الجهاز بطريقة صحيحة.
3. انقل backup عبر وسيط مشفر وتحقق من سلامته.
4. نفّذ Restore واترك الأداة تنشئ Safety Backup وتتحقق من schema والتخزين.
5. تحقق من السجلات المعروفة ومن bytes ملف تخزين معروف ومن `/health`.
6. أعد إعداد LAN/HTTPS وأجهزة العمل؛ هذه حالة تثبيت لا تأتي من backup.

إذا بقي `.dentora-restore-journal.json` بعد انقطاع، لا تحذفه. نفّذ:

```bat
RESTORE_DENTORA.bat --recover
```

## 6. Auto Update

لا تطبق حزمة تحديث يدويًا فوق مجلد العميل. `UPDATE_DENTORA.bat` يتحقق من HTTPS
وتوقيع metadata والحجم وSHA-256 والتوافق، ثم يأخذ backup قبل mutation. عند فشل
البناء أو migration أو health check ينفذ rollback.

إذا بقي `.dentora-update-journal.json`، اترك النظام fail-closed ونفّذ:

```bat
UPDATE_DENTORA.bat --recover
```

لا تغيّر `UPDATE_METADATA_URL` أو `UPDATE_PUBLIC_KEY_B64` على جهاز العميل إلا
ضمن ترحيل Trust Root موثق وموقع من المالك.

## 7. WhatsApp عبر Evolution API v2

Evolution تكامل اختياري ومنفصل عن بيانات Dentora الدائمة. ثبّت إصدارًا محددًا
تمت مراجعته أمنيًا، واستخدم HTTPS، وخصص instance للعيادة. أدخل Base URL واسم
instance وAPI key من شاشة الإعداد المصرح بها. لا تعِد استخدام نفس instance
لعيادات مختلفة.

قبل الإنتاج يلزم تحقق خارجي حقيقي:

1. اتصال instance بحساب WhatsApp المصرح به.
2. إرسال رسالة إلى رقم اختبار بموافقة صاحبه.
3. وصول `MESSAGES_UPDATE` وتحديث delivered/read.
4. اختبار webhook عبر HTTPS والتحقق من السر ومن tenant routing.
5. مراجعة retention والنسخ الاحتياطي الخاصين بـEvolution نفسه.

نجاح اختبارات adapter المحلية لا يثبت التسليم الحقيقي على شبكة WhatsApp.

## 8. Orthodontic Simulator

المحاكي أداة غير سريرية وتبقى fail-closed حتى تتوفر هندسة أسنان خاصة بالمريض
ومراجعة صريحة. لا تستخدم الحركة المعروضة كخطة علاج أو توقع زمني أو بديل عن قرار
أخصائي التقويم. مسار Windows x64 يجب أن يمر من بوابة التوافق الرسمية لنفس
Source SHA قبل التسليم.

## 9. Dentora Voice

الصوت ميزة اختيارية. الـaudio يذهب إلى loopback STT على نفس workstation؛
backend يستقبل النص فقط. نفذ أوامر التنقل/التعبئة ضمن الصلاحيات الحالية وراجع
النتيجة قبل الحفظ. لا تستخدم transcript كتوثيق سريري نهائي دون مراجعة بشرية.

إذا لم تكن خدمة STT المحلية متاحة، عطّل الموديول أو أصلح runtime المحلي؛ لا
تحول الصوت إلى مزود خارجي دون موافقة واضحة وتحديث سياسة الخصوصية.

## 10. AI وDental3D — حدود إلزامية

- Copilot وAI summaries استشارية، ويجب أن تبقى evidence-traceable وتحت مراجعة
  طبيب الأسنان.
- Dental3D وnerve detection وsegmentation لا تثبت دقة تشخيصية أو اعتمادًا
  تنظيميًا أو صلاحية تجارية لأوزان النموذج.
- لا تعرض مسار DICOM فوق هندسة synthetic أو IOS غير مسجلة في patient space.
- لا ترفع بيانات مرضى إلى مزود AI قبل عقد/موافقة وسياسة خصوصية مناسبة.
- النماذج والأوزان الخارجية تحتاج موافقة ترخيص واختبار سريري مستقل قبل الإنتاج.

## 11. القيود المعروفة والتحقق الخارجي المطلوب

تظل العناصر الآتية خارج الإثبات الهندسي المحلي ما لم يوفر العميل بيئتها:

- تفعيل License production الحقيقي وتجديد الـlease من الخدمة الفعلية.
- تنزيل Auto Update من endpoint الإنتاج والتحقق من توقيع صادر بمفتاح المالك.
- إرسال/تسليم WhatsApp حقيقي وwebhook من Evolution متصل.
- توافق أجهزة العيادة الفعلية، الطابعات، الشبكة، وسياسة النسخ المشفر.
- التحقق السريري والتنظيمي والترخيص التجاري لنماذج AI/Dental3D.
- دقة الميكروفون واللهجة والضوضاء على أجهزة العمل الفعلية.

لا تُسجّل أي عنصر من هذه العناصر كناجح بناءً على mock أو بيئة تجريبية فقط.

## 12. استكشاف الأعطال

### الصفحة لا تفتح

تأكد أن Docker Desktop Ready، ثم شغّل `START_DENTORA.bat` وافحص حالة الخدمات.
في وضع Mini PC تحقق من ثبات IP وTCP 443 وشهادة CA على جهاز العمل.

### تحذير شهادة على جهاز عمل

لا تستخدم certificate bypass. أعد إنشاء `Dentora_Workstation_Kit` وانقله كاملًا،
ثم شغّل setup كمسؤول وتحقق من SHA-256 للشهادة.

### فشل migration أو health بعد تحديث

لا تكرر نسخ الملفات يدويًا. استخدم `UPDATE_DENTORA.bat --recover` واحتفظ بالـlog
والjournal بعد تنقيحهما من أي بيانات حساسة.

### فشل Restore

اترك journal والـSafety Backup والـprevious storage volume كما هي. شغّل recovery
ولا تحذف قاعدة أو volume حتى يتم التحقق من الحالة الحية.

### WhatsApp لا يرسل

اختبر اتصال Evolution واسم instance وحالته وHTTPS، ثم راجع provider status
المعقم. لا تضع API key أو payload مريض كاملًا في تذكرة الدعم.

## 13. قائمة تسليم العميل

- [ ] checksum الحزمة مطابق ومستقل.
- [ ] Source SHA في `BUILD_INFO.txt` هو SHA الإصدار المعتمد.
- [ ] لا توجد أسرار أو بيانات مرضى داخل ZIP.
- [ ] Docker Desktop وWindows x64 مستوفيان للمتطلبات.
- [ ] التفعيل وSetup وتسجيل الدخول نجحوا.
- [ ] HTTPS الداخلي وأجهزة العمل نجحت بدون bypass.
- [ ] backup حقيقي أُنشئ وتم التحقق من manifest وchecksums.
- [ ] restore تجريبي أعاد سجلًا وملفًا معروفين بدقة.
- [ ] مسار update وrollback/recovery تم شرحه للمسؤول.
- [ ] RBAC جُرّب بحساب محدود.
- [ ] القيود السريرية والخارجية سُلّمت كتابيًا.
- [ ] مسؤول النسخ الاحتياطي، مكان التخزين المشفر، ودورية الاختبار محددة.
