# Dentora — تثبيت النسخة المرخصة للعيادات على Windows

هذه الحزمة مصممة ليتم استخدام **نفس ملف ZIP** مع أكثر من عيادة، لكن كل تثبيت يحتاج **License Key صالح** قبل إنشاء حساب المسؤول أو استخدام النظام.

بعد التثبيت استخدم `CLIENT_HANDOFF_AR.md` كقائمة التسليم والتشغيل والتعافي
والقيود السريرية والخارجية الموحدة.

## ما يميز النسخة التجارية

- تشغيل Production وليس Dev Server.
- بدون Trial وبدون بيانات Demo.
- كل عيادة لها قاعدة PostgreSQL وملفات ومرفقات مستقلة على جهازها.
- أول تشغيل ينشئ `.env.client` تلقائيًا ويولّد Secrets مختلفة لكل جهاز.
- أول تشغيل يولّد بصمة للجهاز ولا تنقلها إلى عيادة أخرى.
- قبل شاشة Setup تظهر شاشة **تفعيل Dentora**.
- بعد قبول License Key فقط يمكن إنشاء حساب المسؤول وبيانات العيادة.
- العربية هي الواجهة الافتراضية، مع `Africa/Cairo` و`EGP` كإعدادات افتراضية للنسخة المصرية.
- تشغيل وإيقاف ونسخ احتياطي وتحديث من ملفات BAT بدون أوامر يومية.
- الحزمة تثبت مسبقًا عنوان خادم الترخيص ومفتاحه العام، وكذلك عنوان بيانات Auto Update ومفتاح التحقق العام؛ ولا تحتوي على مفاتيح توقيع خاصة.

## المتطلبات

1. Windows 10/11 64-bit.
2. Docker Desktop مثبت ويعمل بوضع Linux containers / WSL2.
3. يفضل 8 GB RAM على الأقل، و16 GB أفضل.
4. مساحة خالية مناسبة لقاعدة البيانات والملفات.
5. اتصال إنترنت عند أول Build وعند التفعيل، ثم يسمح الترخيص بفترة عمل دون اتصال وفق سياسة الترخيص.

## مهم عند استخدام نفس ZIP لأكثر من عميل

- يمكن نقل نفس `Dentora_Client.zip` لأكثر من عميل.
- كل عيادة تحصل منك على **License Key مختلف**.
- فك كل نسخة على جهاز العيادة نفسه في مجلد نظيف مثل `C:\Dentora`.
- لا تنقل `.env.client` أو Docker volumes من عيادة إلى أخرى.
- `START_DENTORA.bat` ينشئ `.env.client` وSecrets وبصمة الجهاز عند أول تشغيل فقط.
- مشاركة ملف ZIP وحده لا تكفي لتشغيل Dentora؛ التثبيت الجديد يحتاج License Key صالح وله عدد Activations محدد.

## التحقق من الحزمة قبل التسليم أو التثبيت

يتم إنشاء ملفين معًا عند تجهيز النسخة الرسمية:

- `Dentora_Client.zip`
- `Dentora_Client.zip.sha256`

على Windows PowerShell احسب بصمة الملف قبل فك الضغط:

```powershell
Get-FileHash .\Dentora_Client.zip -Algorithm SHA256
```

يجب أن تطابق قيمة `Hash` القيمة الأولى الموجودة داخل `Dentora_Client.zip.sha256`. إذا لم تتطابق القيمتان فلا تستخدم الحزمة.

بعد فك الضغط يوجد `BUILD_INFO.txt` ويحتوي على **Source SHA** الذي تم بناء الحزمة منه، بالإضافة إلى عنوان خادم الترخيص وعنوان Auto Update المثبتين وقت التجهيز. لا تعدّل قيم `LICENSE_SERVER_URL` أو `LICENSE_PUBLIC_KEY_B64` أو `UPDATE_METADATA_URL` أو `UPDATE_PUBLIC_KEY_B64` على جهاز العميل؛ هذه Trust Roots عامة ومثبتة داخل الحزمة، بينما Secrets الخاصة بكل تثبيت تبقى فارغة حتى أول تشغيل.

## التثبيت على كمبيوتر العميل

1. فك محتويات `Dentora_Client.zip` إلى `C:\Dentora`.
2. شغّل Docker Desktop وانتظر حتى يصبح Ready.
3. شغّل `START_DENTORA.bat` كالمستخدم العادي.
4. أول Build قد يستغرق عدة دقائق.
5. افتح `http://localhost` إذا لم يفتح المتصفح تلقائيًا.
6. سيتم تحويلك أولًا إلى صفحة **تفعيل Dentora**.
7. أدخل License Key الذي تم إنشاؤه لهذا العميل.
8. بعد نجاح التفعيل ستظهر صفحة Setup.
9. أدخل اسم المسؤول الحقيقي، البريد الإلكتروني، كلمة المرور، اسم العيادة والرقم الضريبي.
10. بعد الدخول أضف حسابات الأطباء والموظفين واضبط جداول العمل والحجز.

## الاستخدام اليومي

- تشغيل النظام: `START_DENTORA.bat`
- إيقاف الخدمات مع الحفاظ على البيانات: `STOP_DENTORA.bat`
- نسخة احتياطية: `BACKUP_DENTORA.bat`
- تحديث موثوق يدويًا عند الحاجة: `UPDATE_DENTORA.bat`

**لا تستخدم `docker compose down -v` على جهاز العميل** لأنه يحذف Docker volumes وبالتالي بيانات العيادة وحالة التفعيل المحلية.

## Auto Update الموثوق

- عميل التحديث يقبل Metadata عبر HTTPS فقط ويتحقق من توقيعها باستخدام المفتاح العام المثبت في الحزمة.
- `UPDATE_DENTORA.bat` يستخدم نفس Trust Root المثبت ولا يحتاج نقل مفتاح توقيع خاص إلى جهاز العميل.
- لا تستبدل `UPDATE_METADATA_URL` أو `UPDATE_PUBLIC_KEY_B64` يدويًا عند العميل.
- التحديث يحتفظ بآلية الاسترجاع عند فشل التثبيت أو فحص الصحة بدل ترك التثبيت في حالة جزئية.
- وجود ملف `Dentora_Client.zip.sha256` يتحقق من سلامة **حزمة التثبيت الأصلية**، بينما توقيع Auto Update يحمي **حزم التحديث اللاحقة**؛ الآليتان مستقلتان ومتكاملتان.

## Mini PC والوصول الآمن من شبكة العيادة

يظل التشغيل الأول متاحًا على `http://localhost` للحفاظ على التوافق مع التثبيتات الحالية. عند استخدام جهاز Mini PC كسيرفر داخل العيادة، يجب استخدام عنوان IPv4 خاص وثابت أو محجوز من الراوتر ثم تفعيل HTTPS على هذا العنوان.

1. ثبّت أو احجز عنوانًا خاصًا للـMini PC، مثل `192.168.1.50`.
2. شغّل `START_DENTORA.bat` مرة واحدة أولًا حتى يتم إنشاء `.env.client` وDocker volumes الخاصة بالتثبيت.
3. افتح Command Prompt بصلاحية Administrator داخل مجلد Dentora وشغّل:

```bat
SETUP_LAN_HTTPS.bat 192.168.1.50
```

السكريبت يتحقق من أن العنوان ضمن شبكات RFC1918 الخاصة، ثم يضبط `PUBLIC_URL` إلى:

```env
PUBLIC_URL=https://192.168.1.50
```

بعد ذلك يعيد Build/Start للواجهة على الـorigin الجديد، ويفتح TCP 80 و443 في Windows Firewall للـ`LocalSubnet` فقط، ويصدر شهادة Caddy المحلية إلى `dentora-lan-ca.crt`، ويضيف هذه الشهادة إلى Trust Store على جهاز الـMini PC، ثم يتحقق فعليًا من `https://<IP>/health`.

- Caddy هو الخدمة الوحيدة المنشورة إلى شبكة العيادة؛ PostgreSQL والـbackend والـfrontend لا تنشر منافذها مباشرة على الـLAN.
- المنفذ 80 موجود فقط لدعم التحويل التلقائي إلى HTTPS عندما يكون `PUBLIC_URL` عنوان HTTPS.
- احتفظ بعنوان الـMini PC ثابتًا. تغيير الـIP يتطلب إعادة تشغيل `SETUP_LAN_HTTPS.bat` بالعنوان الجديد حتى يتطابق الـorigin والشهادة وإعدادات الواجهة.
- ملف `dentora-lan-ca.crt` ليس مفتاحًا خاصًا، لكنه يخص هذا التثبيت ويُنشأ محليًا ولا يتم Commit له في Git.

## Workstation Setup — أجهزة الاستقبال والأطباء

بعد نجاح إعداد الـMini PC وLAN/HTTPS، جهّز حزمة أجهزة العمل على السيرفر بتشغيل:

```bat
PREPARE_WORKSTATION_KIT.bat
```

ينشئ السكريبت `Dentora_Workstation_Kit` ويضع داخله شهادة الـCA العامة، إعدادات URL وSHA-256، سكريبت إعداد جهاز العمل، ودليل الاستخدام فقط. لا ينسخ `.env.client` أو كلمات مرور قاعدة البيانات أو مفاتيح خاصة أو بيانات مرضى.

على كل جهاز استقبال أو طبيب:

1. انسخ مجلد `Dentora_Workstation_Kit` كاملًا من الـMini PC.
2. شغّل `SETUP_DENTORA_WORKSTATION.bat` بصلاحية Administrator.
3. السكريبت يتحقق من URL الخاص بالـMini PC ومن سلامة شهادة الـCA باستخدام SHA-256، ويتأكد من الوصول إلى TCP 443.
4. يثبت شهادة Dentora في Windows `LocalMachine\Root` ويختبر `/health` على HTTPS **بدون certificate bypass**.
5. ينشئ Shortcut باسم **Dentora Clinic** على Public Desktop لكل مستخدمي الجهاز.

أجهزة العمل لا تحتاج Docker أو PostgreSQL أو نسخة محلية من Dentora أو `.env.client`. راجع `WORKSTATION_SETUP_AR.md` للتفاصيل واستكشاف الأخطاء.

## الترخيص والعمل دون إنترنت

- مفتاح الترخيص لا يُخزن كنص واضح بعد التفعيل.
- Dentora يحتفظ محليًا بLease موقع رقميًا من خادم الترخيص.
- البرنامج يجدد الـLease عند توفر الإنترنت.
- إذا تعذر الاتصال مؤقتًا، يستمر العمل حتى نهاية فترة السماح الموجودة في الـLease.
- بعد انتهاء الـLease دون إمكانية التجديد، تتوقف وظائف النظام المحمية حتى عودة الاتصال أو معالجة الترخيص.
- إيقاف أو تعليق License من خادم المالك يظهر على العميل عند التجديد التالي، وبحد أقصى عند انتهاء فترة السماح الحالية.

## ملاحظات مهمة

- كل عيادة مستقلة عن غيرها في البيانات والترخيص.
- الحجز الإلكتروني يمكن تفعيله بعد إنشاء الأطباء وجداول العمل.
- AI Copilot اختياري؛ ضع مفتاح OpenAI في `OPENAI_API_KEY` داخل `.env.client` فقط إذا كان سيتم استخدامه.
- احتفظ بنسخ احتياطية دورية خارج نفس القرص.
