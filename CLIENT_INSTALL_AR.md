# DentalPin — تثبيت النسخة العامة للعيادات على Windows

هذه الحزمة مصممة ليتم استخدام **نفس ملف ZIP** مع أكثر من عيادة. كل عيادة تثبّت النسخة في مجلد مستقل، وعند أول تشغيل يتم إنشاء إعدادات وأسرار خاصة بهذه العيادة فقط.

## ما يميز النسخة العامة

- تشغيل Production وليس Dev Server.
- بدون Trial.
- بدون بيانات Demo.
- قاعدة PostgreSQL وملفات الأشعة/المرفقات محفوظة في Docker volumes دائمة لكل تثبيت.
- أول تشغيل ينشئ `.env.client` تلقائيًا ويولّد Secrets مختلفة لكل عيادة.
- أول تشغيل يفتح شاشة إعداد الحساب الإداري الأول واسم العيادة.
- العربية هي الواجهة الافتراضية، مع `Africa/Cairo` و`EGP` كإعدادات افتراضية مناسبة للعيادات في مصر.
- يمكن إنشاء مستخدمين وأطباء مختلفين لكل عيادة بعد الدخول.
- تشغيل وإيقاف ونسخ احتياطي من ملفات BAT بدون أوامر يومية.

## المتطلبات

1. Windows 10/11 64-bit.
2. Docker Desktop مثبت ويعمل بوضع Linux containers / WSL2.
3. يفضل 8 GB RAM على الأقل، و16 GB أفضل.
4. مساحة خالية مناسبة لقاعدة البيانات والملفات.
5. اتصال إنترنت في أول Build لتنزيل Docker images وdependencies.

## مهم عند استخدام نفس ZIP لأكثر من عميل

- انقل نفس `DentalPin_Generic_Client.zip` لكل عميل.
- فك كل نسخة على **جهاز العيادة نفسه** في مجلد نظيف مثل `C:\DentalPin`.
- لا تنقل ملف `.env.client` من عيادة إلى عيادة أخرى.
- لا تنقل Docker volumes أو قاعدة بيانات عيادة إلى عيادة أخرى.
- `START_DENTALPIN.bat` ينشئ `.env.client` وSecrets جديدة تلقائيًا عند أول تشغيل فقط.
- إذا كان `.env.client` موجودًا بالفعل فلن يتم تغييره، حتى تظل قاعدة البيانات قابلة للعمل بنفس كلمة المرور والمفاتيح.

## التثبيت على كمبيوتر العميل

1. فك محتويات `DentalPin_Generic_Client.zip` إلى مكان ثابت، مثل:
   `C:\DentalPin`
2. شغّل Docker Desktop وانتظر حتى يصبح Ready.
3. شغّل `START_DENTALPIN.bat` كالمستخدم العادي.
4. في أول تشغيل سيولّد DentalPin إعدادات التثبيت الخاصة بهذه العيادة تلقائيًا.
5. أول Build قد يستغرق عدة دقائق.
6. افتح `http://localhost` إذا لم يفتح المتصفح تلقائيًا.
7. سيتم تحويلك إلى صفحة Setup لأن قاعدة البيانات نظيفة.
8. أدخل اسم المسؤول الحقيقي، البريد الإلكتروني، كلمة المرور، اسم العيادة، والرقم الضريبي الخاص بهذه العيادة.
9. بعد الدخول أضف حسابات الأطباء والموظفين الخاصة بهذه العيادة من إدارة المستخدمين، ثم اضبط جداول العمل والحجز حسب احتياجها.

## الاستخدام اليومي

- تشغيل النظام: `START_DENTALPIN.bat`
- إيقاف الخدمات مع الحفاظ على البيانات: `STOP_DENTALPIN.bat`
- نسخة احتياطية لقاعدة البيانات والمرفقات: `BACKUP_DENTALPIN.bat`

**لا تستخدم `docker compose down -v` على جهاز العميل** لأنه يحذف Docker volumes وبالتالي بيانات العيادة.

## الوصول من أجهزة أخرى داخل العيادة

إذا كان مطلوبًا فتح DentalPin من أجهزة أخرى على نفس الشبكة، ثبّت IP لجهاز السيرفر ثم غيّر في `.env.client`:

```env
PUBLIC_URL=http://192.168.1.50
```

واسمح بمنفذ TCP 80 في Windows Firewall. بعد ذلك أعد تشغيل الخدمات.

## ملاحظات مهمة

- كل عيادة مستقلة تمامًا عن العيادات الأخرى طالما كل تثبيت له `.env.client` وDocker volumes الخاصة به.
- بيانات أي نسخة Railway أو Demo لا تنتقل تلقائيًا إلى هذه النسخة.
- الحجز الإلكتروني يمكن تفعيله بعد إنشاء الأطباء وجداول العمل.
- AI Copilot اختياري؛ ضع مفتاح OpenAI في `OPENAI_API_KEY` داخل `.env.client` فقط إذا كان سيتم استخدامه.
- احتفظ بنسخ احتياطية دورية خارج نفس القرص الذي يعمل عليه النظام.
