# Dentora — تثبيت النسخة المرخصة للعيادات على Windows

هذه الحزمة مصممة ليتم استخدام **نفس ملف ZIP** مع أكثر من عيادة، لكن كل تثبيت يحتاج **License Key صالح** قبل إنشاء حساب المسؤول أو استخدام النظام.

## ما يميز النسخة التجارية

- تشغيل Production وليس Dev Server.
- بدون Trial وبدون بيانات Demo.
- كل عيادة لها قاعدة PostgreSQL وملفات ومرفقات مستقلة على جهازها.
- أول تشغيل ينشئ `.env.client` تلقائيًا ويولّد Secrets مختلفة لكل جهاز.
- أول تشغيل يولّد بصمة للجهاز ولا تنقلها إلى عيادة أخرى.
- قبل شاشة Setup تظهر شاشة **تفعيل Dentora**.
- بعد قبول License Key فقط يمكن إنشاء حساب المسؤول وبيانات العيادة.
- العربية هي الواجهة الافتراضية، مع `Africa/Cairo` و`EGP` كإعدادات افتراضية للنسخة المصرية.
- تشغيل وإيقاف ونسخ احتياطي من ملفات BAT بدون أوامر يومية.

## المتطلبات

1. Windows 10/11 64-bit.
2. Docker Desktop مثبت ويعمل بوضع Linux containers / WSL2.
3. يفضل 8 GB RAM على الأقل، و16 GB أفضل.
4. مساحة خالية مناسبة لقاعدة البيانات والملفات.
5. اتصال إنترنت عند أول Build وعند التفعيل، ثم يسمح الترخيص بفترة عمل دون اتصال وفق سياسة الترخيص.

## مهم عند استخدام نفس ZIP لأكثر من عميل

- يمكن نقل نفس `Dentora_Client.zip` لأكثر من عميل.
- كل عيادة تحصل منك على **License Key مختلف**.
- فك كل نسخة على جهاز العيادة نفسه في مجلد نظيف مثل `C:\Dentora`.
- لا تنقل `.env.client` أو Docker volumes من عيادة إلى أخرى.
- `START_DENTORA.bat` ينشئ `.env.client` وSecrets وبصمة الجهاز عند أول تشغيل فقط.
- مشاركة ملف ZIP وحده لا تكفي لتشغيل Dentora؛ التثبيت الجديد يحتاج License Key صالح وله عدد Activations محدد.

## التثبيت على كمبيوتر العميل

1. فك محتويات `Dentora_Client.zip` إلى `C:\Dentora`.
2. شغّل Docker Desktop وانتظر حتى يصبح Ready.
3. شغّل `START_DENTORA.bat` كالمستخدم العادي.
4. أول Build قد يستغرق عدة دقائق.
5. افتح `http://localhost` إذا لم يفتح المتصفح تلقائيًا.
6. سيتم تحويلك أولًا إلى صفحة **تفعيل Dentora**.
7. أدخل License Key الذي تم إنشاؤه لهذا العميل.
8. بعد نجاح التفعيل ستظهر صفحة Setup.
9. أدخل اسم المسؤول الحقيقي، البريد الإلكتروني، كلمة المرور، اسم العيادة والرقم الضريبي.
10. بعد الدخول أضف حسابات الأطباء والموظفين واضبط جداول العمل والحجز.

## الاستخدام اليومي

- تشغيل النظام: `START_DENTORA.bat`
- إيقاف الخدمات مع الحفاظ على البيانات: `STOP_DENTORA.bat`
- نسخة احتياطية: `BACKUP_DENTORA.bat`

**لا تستخدم `docker compose down -v` على جهاز العميل** لأنه يحذف Docker volumes وبالتالي بيانات العيادة وحالة التفعيل المحلية.

## الوصول من أجهزة أخرى داخل العيادة

إذا كان مطلوبًا فتح Dentora من أجهزة أخرى على نفس الشبكة، ثبّت IP لجهاز السيرفر ثم غيّر في `.env.client`:

```env
PUBLIC_URL=http://192.168.1.50
```

واسمح بمنفذ TCP 80 في Windows Firewall ثم أعد تشغيل الخدمات.

## الترخيص والعمل دون إنترنت

- مفتاح الترخيص لا يُخزن كنص واضح بعد التفعيل.
- Dentora يحتفظ محليًا بLease موقع رقميًا من خادم الترخيص.
- البرنامج يجدد الـLease عند توفر الإنترنت.
- إذا تعذر الاتصال مؤقتًا، يستمر العمل حتى نهاية فترة السماح الموجودة في الـLease.
- بعد انتهاء الـLease دون إمكانية التجديد، تتوقف وظائف النظام المحمية حتى عودة الاتصال أو معالجة الترخيص.
- إيقاف أو تعليق License من خادم المالك يظهر على العميل عند التجديد التالي، وبحد أقصى عند انتهاء فترة السماح الحالية.

## ملاحظات مهمة

- كل عيادة مستقلة عن غيرها في البيانات والترخيص.
- الحجز الإلكتروني يمكن تفعيله بعد إنشاء الأطباء وجداول العمل.
- AI Copilot اختياري؛ ضع مفتاح OpenAI في `OPENAI_API_KEY` داخل `.env.client` فقط إذا كان سيتم استخدامه.
- احتفظ بنسخ احتياطية دورية خارج نفس القرص.
