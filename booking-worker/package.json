{
  "name": "dentalpin-booking-worker",
  "version": "0.1.0",
  "private": true,
  "description": "DentalPin Public Booking Cloud on Cloudflare Workers + D1",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.44.0"
  }
}
